import 'package:flutter/material.dart';

class AppColor {
  final Color? primaryColor;
  final Color? disableButtonColor;
  final Color? warningColor;
  AppColor({this.primaryColor, this.disableButtonColor, this.warningColor});
}
